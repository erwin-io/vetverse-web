import { NumberLeadZeroPipePipe } from './number-lead-zero.pipe.pipe';

describe('NumberLeadZeroPipePipe', () => {
  it('create an instance', () => {
    const pipe = new NumberLeadZeroPipePipe();
    expect(pipe).toBeTruthy();
  });
});
